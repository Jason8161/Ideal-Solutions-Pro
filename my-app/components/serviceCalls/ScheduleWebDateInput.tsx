import type { ReactNode } from "react";

/** On native, visit date uses DateTimePicker in the parent screen. */
export function ScheduleWebDateInput(_props: { value: string; onChange: (dayKey: string) => void }): ReactNode {
  return null;
}
