import { SupplierHubScreen } from "@/components/supplierHub/SupplierHubScreen";

/** Supplier Hub — open supplier apps and websites (route kept as /materials-search). */
export default function MaterialsSearchScreen() {
  return <SupplierHubScreen />;
}
